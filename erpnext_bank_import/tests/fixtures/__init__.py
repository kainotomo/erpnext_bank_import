# Shared test fixtures for erpnext_bank_import
#
# This package provides reusable factory functions, data builders, and
# mock objects shared across all test modules.  The pytest fixtures
# defined in ``conftest.py`` use these helpers internally.
#
# Sub-modules
# -----------
# * ``oauth_fixtures`` — OAuth token dicts, ``OAuth2Service`` factory
# * ``import_fixtures`` — Mock connectors, mappings, patch helpers

from __future__ import annotations
